<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.noldangGapseo.dao.NoticeBoardDao">
    <select id="getNoticeList" resultType="NoticeVO">
        select * from noldang.service_center a , noldang.user b 
        where a.user_id = b.user_id and a.service_type = #{type}  
    </select>
    
    <insert id="addQ"> <!-- 질문등록 -->
        
insert into noldang.service_center (service_center_id, user_id, service_type, title, contents, view_count, reg_date, update_date)
values( service_center_id ,5 , 'Q', #{title}, #{content} , 0 , now(), now())
        
    </insert>
</mapper>